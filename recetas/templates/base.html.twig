<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Recetas{% endblock %}</title>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background-color: #f6f8f5;
            font-family: 'Segoe UI', Tahoma, sans-serif;
            color: #4a4a4a;
        }

        nav {
            background-color: #e8f0e4;
            padding: 12px 20px;
            border-radius: 12px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        nav a {
            text-decoration: none;
            font-weight: 500;
            margin-left: 15px;
        }

        nav a:hover {
            text-decoration: underline;
        }

        nav .nav-left {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        nav .usuario {
            font-weight: 600;
            color: #3f5f4f;
        }

        nav .btn-logout {
            background-color: #f5b5b5;
            color: #fff;
            border: none;
            padding: 5px 10px;
            border-radius: 5px;
        }

        nav .btn-logout:hover {
            background-color: #f09a9a;
        }

        hr {
            border: none;
            height: 1px;
            background-color: #d6e2d3;
            margin: 20px 0;
        }

        .card {
            border-radius: 16px;
            border: none;
            background-color: #ffffff;
        }

        .card-title {
            color: #5a7d6a;
            font-weight: 600;
        }

        .form-control {
            border-radius: 10px;
            border: 1px solid #d6e2d3;
        }

        .form-control:focus {
            border-color: #9fc4aa;
            box-shadow: 0 0 0 0.2rem rgba(159, 196, 170, 0.25);
        }

        .btn-primary {
            background-color: #9fc4aa;
            border: none;
        }

        .btn-primary:hover {
            background-color: #88b29a;
        }

        .btn-success {
            background-color: #f2b5a7;
            border: none;
        }

        .btn-success:hover {
            background-color: #e89f8e;
        }

        a {
            color: #7aa6a1;
        }

        a:hover {
            color: #5f8f89;
        }

        .alert-danger {
            border-radius: 10px;
        }

        .site-title {
            font-family: 'Georgia', serif;
            font-size: 28px;
            font-weight: bold;
        }
    </style>
</head>

<body>

<nav>
    <div class="nav-left">
        {% if usuario %}
            <span class="usuario">{{ usuario.name|default(usuario.email) }}</span>
            <a href="/public/miperfil.php">Mi Perfil</a>
            <a href="/public/recetas.php">Recetas</a>
            <a href="/public/logout.php" class="btn-logout">Cerrar sesión</a>
        {% else %}
            <a href="/public/login.php">Login</a>
            <a href="/public/register.php">Registro</a>
        {% endif %}
    </div>

    <div class="site-title">RECETAS DE COCINA</div>
</nav>

<hr>

<div class="container mt-4">
    {% block content %}{% endblock %}
</div>

</body>
</html>
